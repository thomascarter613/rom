[package]
name = "monad-repo-model"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Repository Object Model types for Monad OS repository control plane exploration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
